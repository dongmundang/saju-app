services:
  - type: web
    name: dongmundang
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      # 배포 후 Render 대시보드에서 ANTHROPIC_API_KEY 를 직접 추가하세요 (여기 적지 마세요)
